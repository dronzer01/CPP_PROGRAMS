#include <iostream>
using namespace std;
void getinput(int x[],int m)
{
    cout<<"enter elements in array x";
    for(int i=0;i<m;i++)
        cin>>x[i];
}
void binarysearch( int x[],int m)
{
    int p;
    cout<<"enter the element u wan to search\n";
    cin>>p;
    while low=0;

int main()
{
    int a[20];
    int n;
    cout<<"number of elements in array a";
    cin>>n;
    getinput(a,n);
    binarysearch(a,n);
    return 0;
}
